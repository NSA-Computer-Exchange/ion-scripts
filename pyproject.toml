[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "ion-cli"
version = "0.1.0"
description = "ION scripting CLI"

[project.scripts]
ion = "ioncli.cli:main"

[tool.setuptools]
packages = ["ioncli", "security"]